model:
  hf_name: PekingU/rtdetr_r50vd
  local_path: pretrained/rtdetr_r50vd
  cache_dir:
  device: auto
  threshold: 0.3

input:
  image_path:

output:
  output_dir: outputs/rtdetr_baseline
  save_features: true
  save_visualization: true